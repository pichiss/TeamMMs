import './main.css'
import back from '../../../img/background_boss.png'

function Main(){
    return(
    <main style={{backgroundImage : `url(${back})`}}>
        
    </main>
    )
}

export default Main